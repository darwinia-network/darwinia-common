[package]
authors     = ["Darwinia Network <hello@darwinia.network>"]
description = "Ethereum Primitives"
edition     = "2018"
homepage    = "https://darwinia.network/"
license     = "GPL-3.0"
name        = "ethereum-primitives"
readme      = "README.md"
repository  = "https://github.com/darwinia-network/darwinia-common/"
version     = "2.6.9"

[dependencies]
# crates.io
array-bytes     = { version = "1.3.3" }
codec           = { package = "parity-scale-codec", version = "2.1.1", optional = true, default-features = false, features = ["derive"] }
ethbloom        = { version = "0.11.0", default-features = false }
ethereum-types  = { version = "0.11.0", default-features = false }
keccak-hash     = { version = "0.7.0", default-features = false }
rlp             = { version = "0.5.0", optional = true, default-features = false }
rlp-derive      = { version = "0.1.0", optional = true }
serde           = { version = "1.0.127", optional = true, features = ["derive"] }
sp-debug-derive = { version = "3.0.0" }
subhasher       = { version = "0.1.7", default-features = false }
# darwinia-network
ethash               = { default-features = false, git = "https://github.com/darwinia-network/rust-ethash", tag = "v0.4.0" }
merkle-patricia-trie = { default-features = false, path = "../merkle-patricia-trie" }

[dev-dependencies]
# crates.io
array-bytes    = { version = "1.3.3", features = ["serde"] }
codec          = { package = "parity-scale-codec", version = "2.1.1", features = ["derive"] }
ethbloom       = { version = "0.11.0", features = ["codec"] }
ethereum-types = { version = "0.11.0", features = ["codec"] }
keccak-hasher  = { version = "0.15.3" }
rlp            = { version = "0.5.0" }
rlp-derive     = { version = "0.1.0" }
serde          = { version = "1.0.127", features = ["derive"] }
serde_json     = { version = "1.0.66" }
triehash       = { version = "0.8.4" }

[features]
default = ["std"]

std = [
	# crates.io
	"ethbloom/std",
	"ethereum-types/std",
	"keccak-hash/std",
	"sp-debug-derive/std",
	"subhasher/std",
	# darwinia-network
	"ethash/std",
	"merkle-patricia-trie/std",
]

full-codec = [
	"codec",
	"ethbloom/codec",
	"ethereum-types/codec",
]

full-rlp = [
	"rlp",
	"rlp-derive",
	"ethbloom/rlp",
	"ethereum-types/rlp",
]

full-serde = [
	"array-bytes/serde",
	"serde",
	"ethbloom/serialize",
	"ethereum-types/serialize",
]

dag = []
